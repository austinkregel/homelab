services:
  prometheus:
    container_name: prometheus
    image: prom/prometheus
    volumes:
      - ../appdata/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
      - ../appdata/prometheus/rules.yml:/etc/prometheus/rules.yml
      - ../appdata/prometheus:/etc/prometheus
    networks:
      - internal
  node-exporter:
    container_name: node-exporter
    image: prom/node-exporter
    networks:
      - internal
  alert-manager:
    image: prom/alertmanager
    volumes:
      - ../appdata/prometheus/alertmanager.yml:/alertmanager/alertmanager.yml"
    networks:
      - internal
  grafana:
    container_name: grafana
    image: grafana/grafana:latest
    user: '1000'
    volumes:
      - ../appdata/grafana:/var/lib/grafana
    networks:
      - internal
    restart: unless-stopped
  influxdb:
    container_name: influxdb
    image: influxdb:2
    volumes:
      - ../appdata/influxdb/data:/var/lib/influxdb
      - ../appdata/influxdb/config:/etc/influxdb
    networks:
      - internal
    restart: unless-stopped
    environment:
      DOCKER_INFLUXDB_INIT_MODE: setup
      DOCKER_INFLUXDB_INIT_USERNAME_FILE: /run/secrets/influxdb2-admin-username
      DOCKER_INFLUXDB_INIT_PASSWORD_FILE: /run/secrets/influxdb2-admin-password
      DOCKER_INFLUXDB_INIT_ADMIN_TOKEN_FILE: /run/secrets/influxdb2-admin-token
      DOCKER_INFLUXDB_INIT_ORG: docs
      DOCKER_INFLUXDB_INIT_BUCKET: home
    secrets:
      - influxdb2-admin-username
      - influxdb2-admin-password
      - influxdb2-admin-token
secrets:
  influxdb2-admin-username:
    file: ~/.env.influxdb2-admin-username
  influxdb2-admin-password:
    file: ~/.env.influxdb2-admin-password
  influxdb2-admin-token:
    file: ~/.env.influxdb2-admin-token